---
import '../styles/global.css';

interface Props {
  title?: string;
}

const { title = 'Moonshot Forge — Web3 Venture Studio' } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Moonshot Forge is a full-stack Web3 venture studio that co-founds, funds, and builds the infrastructure protocols defining the decentralized economy." />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
  </head>
  <body class="bg-forge-black text-forge-white font-mono font-light">
    <!-- Custom cursor (desktop only) -->
    <div class="cursor" id="cursor"></div>
    <div class="cursor-ring" id="cursorRing"></div>

    <slot />

    <!-- Toast -->
    <div class="toast" id="toast">✓ Application submitted — we'll be in touch within 5 business days.</div>
  </body>
</html>
